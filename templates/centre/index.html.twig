{% extends 'base.html.twig' %}

{% block title %}Centre
{% endblock %}

{% block body %}

	<div class="example-wrapper d-flex justify-content-center align-items-center flex-column" style="min-height: 100vh;">
		<div class="text-center">
			<h1>Bienvenue
				{{ app.user.username }}, dans "Nains !", le jeu par navigateur.</h1>
			<p>Le jeu est actuellement en construction ! N'hésitez pas à faire un tour sur le forum !</p>
		</div>

		<div class="mt-3 d-flex flex-column align-items-center">
			<a href="{{ path('app_forum') }}" class="btn btn-primary mb-3">Forum</a>
			<a href="{{ path('app_logout') }}" class="btn btn-secondary">Déconnexion</a>
		</div>
	</div>

{% endblock %}
