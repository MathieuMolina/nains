{% extends 'base.html.twig' %}

{% block title %}Topic{% endblock %}

{% block body %}
    <h1>Topic</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ topic.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ topic.title }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_topic_index') }}">Retour à la liste</a>

    <a href="{{ path('app_topic_edit', {'id': category.id}) }}">edit</a>

    {{ include('topic/_delete_form.html.twig') }}
{% endblock %}